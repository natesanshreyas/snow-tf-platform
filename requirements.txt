# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Microsoft Agent Framework (pre-GA — RC5 as of March 2026)
# https://github.com/microsoft/agent-framework
agent-framework --pre

# Azure identity (for managed identity / service principal auth)
azure-identity>=1.19.0

# HTTP client
httpx>=0.27.0

# Environment
python-dotenv>=1.0.1
